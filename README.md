# Basic Sappeur Demo App

```
This is a basic app developed in Sappeur language
```

## How to execute

run `./callCompiler.sh`

this will create `output` directory.

run the following commands:

```
cd output
make all
```

to run the app:

```
./sprprog.exe
```
